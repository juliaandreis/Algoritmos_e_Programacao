/*Gerar e escrever os números pares do intervalo [20,50]*/

#include <stdio.h>

int main ()
{
    int cont = 20;
    while (cont <= 50)
    {
        printf("%d\n", cont);
        cont ++;
    }

    return 0;
}