/*Gerar e escrever todos os números inteiros do intervalo [0,100].*/

#include <stdio.h>

int main ()
{
    int cont = 0;
    while (cont <= 100)
    {
        printf("%d\n", cont);
        cont ++;
    }

    return 0;
}