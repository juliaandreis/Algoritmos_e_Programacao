#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main ()
{
    srand(time(0));
    int psHamster1=0; posHamster2=0;
    bool acabou = false;
    while (acabou == false) // pode colocar tbm "!acabou"
    {
        //Movimentação hamster 1
        int n = rand() % 5 + 1; // sorteia um valor entre 1 e 5
        if (n == 1)
        {
            posHamster1 = 
        }
    }



    return 0;
}